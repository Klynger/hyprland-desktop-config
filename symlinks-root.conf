# ============================================================
# ROOT-LEVEL SYMLINKS — requires sudo
# ============================================================
# These symlinks target system directories (e.g. /usr/share)
# that are owned by root. They CANNOT be created with the
# regular symlinks.sh script.
#
# Usage:
#   sudo ./scripts/symlinks-root.sh --create
#   sudo ./scripts/symlinks-root.sh --delete
# ============================================================

# SDDM Material You theme
$(pwd)/sddm/material-you:/usr/share/sddm/themes/material-you
